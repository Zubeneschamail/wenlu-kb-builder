# 当前验证记录

## 一键导入闻录（2026-09-22）

- Builder 检索区新增「一键导入闻录」，支持当前生成的包与已打开的包；导入过程等待闻录持久化确认，期间禁用重复点击和取消。
- 闻录新增本机接收服务、受令牌保护的请求、Tk 主线程应用设置、独立目录复制、同包更新和失败回滚；设置窗口正在编辑、跨客户及数量上限会拒绝导入。
- 已实际点击 Builder 按钮，经真实本机 HTTP 交给真实闻录 App，验证 `.wlkb` 复制、参考资料设置落盘、启用开关及界面资料列表同步。该测试使用隔离的临时资料目录，没有向用户资料列表导入测试包。
- 新增 Builder 导入通信/启动流程 5 项、按钮 1 项，以及闻录导入与回滚 6 项测试。
- 本轮完整回归通过：Builder 45 项，闻录 297 项。实际导入成功界面已截图检查，按钮位于检索区右上角。
- 本机已登记当前源码版闻录的启动位置，Builder 可自动发现；未向用户当前参考资料列表添加验证包。
- 当前源码版支持此接收协议；此前生成的闻录安装包尚未包含接收服务，需要重新打包更新后使用。

## 动态网页兼容（2026-09-22）

- FairyGUI `https://www.fairygui.com/docs/editor` 的 HTTP 响应只有 Vue 页面框架，无正文；已复现原版提取失败。
- 新增自动后台浏览器回退：正文缺失/哈希路由时通过本机 Edge、Chrome 或已安装的 Chromium 执行 JavaScript，等待正文稳定和脚本/数据请求结束，再交由现有正文提取器处理。
- 真实 Edge 无头浏览器已抓取上述页面并生成 `output/fairygui-editor.wlkb`：38 个片段；使用闻录实际编码器查询“2.x 版本项目如何打开”，命中打开文件夹的正文，并保留原 URL。未调用在线问答 API。
- 新增 6 项浏览器回归：实际本地 HTTP + 延迟 JavaScript/XHR + 哈希路由、无浏览器提示、静态/登录页不启动浏览器、超时与取消关闭浏览器、渲染失败保留旧包。全套共 39 项测试通过，`pip check` 通过。
- 新增 Playwright 及依赖锁；本机已安装并使用现有 Edge，无需额外下载浏览器。此功能只导入当前章节，不递归抓取文档目录。

## 网页导入（2026-09-22）

- Builder 完整测试 33 项通过；新增网页真实正文提取、中文编码、去导航页脚、锚点清理、网址去重、重定向、HTTP 错误与重试、超时、压缩响应大小限制、取消、GUI 网页生成与来源显示、CLI 网址清单、混合资料构建及更新复用测试。
- 闻录知识包相关测试 13 项通过，包含网页来源传入回答上下文、不安全 URL 拒绝和旧包兼容。未调用付费问答 API。
- 实际通过网络抓取 Python 官方中文虚拟环境文档，生成 `output/web-live-check.wlkb`，并通过闻录实际编码器检索；验证 URL 与抓取时间随片段进入上下文。测试产物在忽略的 output 目录，不进入发行包。
- 已检查真实 Tk 网页入口和检索结果截图：100%（960×780）、125%（860×740），并自动验证最小窗口下至少四行结果区域、输入框对齐和页签切换。
- 新增正文提取依赖及锁定版本；本机已安装，`pip check` 通过。其他电脑更新源码后需运行 `setup.cmd`。
- 本轮未重新打包闻录安装程序；闻录源码已支持网页来源信息，旧版安装程序仍可读取包内正文。

## 此前界面更新

界面更新验证日期：2026-09-22。工具位置：`E:\wenlu-kb-builder`。

- 此前界面版本完整测试共 20 项通过，包括 16 项真实模型构建器测试和 4 项 GUI 流程测试。
- GUI 生成和检索使用测试时临时创建的资料，界面已移除 Demo 入口，不再依赖样例填充。
- 已验证阶段进度、真实模型生成检索、`.wldb` 路径纠错、文件缺失提示、超时失败、取消、重复任务拦截及操作恢复。
- 沿用闻录细滚动条与扁平页签；知识库工具使用独立的紫色书本图标，同步标题栏、任务栏和页头。ICO 含 16、20、24、32、40、48、64、128、256 像素共九个尺寸，各尺寸解码检查通过。
- 第二轮布局已验证 860×680 的最小窗口和 100% / 125% 字体缩放，表单输入框左边缘一致，结果区至少容纳四行文字。
- 已在实际 Tk 测试窗口完成待机、阶段进度及真实生成/检索结果截图检查（960×760 和 860×680）。截图位于本地 `output/ui-v2-*.png`，不纳入发布。

## 初版功能验证（2026-09-21）

- 独立 Python 3.12 环境已在 E 盘重建启动脚本；依赖已安装并写入 `requirements-lock.txt`。
- BGE 中文小模型的固定 ONNX 量化版本已实际下载，三个模型文件均通过固定 SHA256 校验。
- 已实际构建 `output/linzhou-demo.wlkb`：1 个虚构资料文件，16 个片段，每个向量为 512 维。
- 构建器 16 项真实模型回归测试通过：包括语义检索、向量归一化、增量复用、删除资料、客户隔离、模型版本不匹配、失败与取消保留旧包、长文本、DOCX、PDF 和输出后缀仅接受 `.wlkb`。
- 初版窗口组件流程测试通过；该版本共 17 项功能测试通过，当前界面已按上方记录更新。
- 实际 CLI 重复构建返回 `unchanged: true`，保持知识包版本 1；实际 CLI 查询结果保存至 `output/sample-search.json`。
- `pip check` 通过。
- 10 道独立 demo 检索题中，9 道在 Top5 覆盖全部标注章节。Q14「两个项目里面 Redis 的用途有什么不同」漏掉 S06。详见 `output/demo-evaluation.json`。
- 上述检索测试不是答案准确率，也不是生产规模评测；未进行在线问答或商业效果验证。
- 窗口能够启动，系统报告响应正常。截图辅助工具初始化两次失败，错误为「failed to write kernel assets: 系统找不到指定的路径。(os error 3)」，因此未完成视觉布局检查。
- 工具已迁至 E 盘，与闻录仓库独立。闻录已完成 `.wlkb` 加载适配，打包程序已实际读取样例并检索到 S08；demo 和客户知识包不参与闻录打包。
