# 当前验证记录

验证日期：2026-09-21。工具位置：`E:\wenlu-kb-builder`。

- 独立 Python 3.12 环境已在 E 盘重建启动脚本；依赖已安装并写入 `requirements-lock.txt`。
- BGE 中文小模型的固定 ONNX 量化版本已实际下载，三个模型文件均通过固定 SHA256 校验。
- 已实际构建 `output/linzhou-demo.wlkb`：1 个虚构资料文件，16 个片段，每个向量为 512 维。
- 构建器 16 项真实模型回归测试通过：包括语义检索、向量归一化、增量复用、删除资料、客户隔离、模型版本不匹配、失败与取消保留旧包、长文本、DOCX、PDF 和输出后缀仅接受 `.wlkb`。
- 另有 1 项窗口组件流程测试通过：从「填入虚构样例」到后台生成，再检索并展示 S08 与 outbox 原文；窗口隐藏运行，因此不等同于视觉验收。共 17 项功能测试通过。
- 实际 CLI 重复构建返回 `unchanged: true`，保持知识包版本 1；实际 CLI 查询结果保存至 `output/sample-search.json`。
- `pip check` 通过。
- 10 道独立 demo 检索题中，9 道在 Top5 覆盖全部标注章节。Q14「两个项目里面 Redis 的用途有什么不同」漏掉 S06。详见 `output/demo-evaluation.json`。
- 上述检索测试不是答案准确率，也不是生产规模评测；未进行在线问答或商业效果验证。
- 窗口能够启动，系统报告响应正常。截图辅助工具初始化两次失败，错误为「failed to write kernel assets: 系统找不到指定的路径。(os error 3)」，因此未完成视觉布局检查。
- 工具已迁至 E 盘，与闻录仓库独立。闻录已完成 `.wlkb` 加载适配，打包程序已实际读取样例并检索到 S08；demo 和客户知识包不参与闻录打包。
